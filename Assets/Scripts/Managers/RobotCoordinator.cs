using UnityEngine;

namespace WarehouseSimulation.Managers
{
    /// <summary>
    /// Coordinates robot agents and shared movement/task orchestration.
    /// Keeps robot control concerns separated from game and task management.
    /// </summary>
    public class RobotCoordinator : MonoBehaviour
    {
        // Reserved for future robot registry setup.
        private void Awake()
        {
        }

        // Reserved for future coordination routines.
        private void FixedUpdate()
        {
        }
    }
}
