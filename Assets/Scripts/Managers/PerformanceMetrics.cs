using UnityEngine;

namespace WarehouseSimulation.Managers
{
    /// <summary>
    /// Collects and exposes simulation KPIs (throughput, utilization, wait times).
    /// Structured as a standalone component for easy extension and UI integration.
    /// </summary>
    public class PerformanceMetrics : MonoBehaviour
    {
        // Reserved for future metrics initialization.
        private void Awake()
        {
        }

        // Reserved for future sampling and reporting cadence.
        private void LateUpdate()
        {
        }
    }
}
